services:

  postgres:
    image: postgres:16
    container_name: postgres-db
    restart: always

    environment:
      POSTGRES_DB: cloudadmin
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    container_name: backend-api
    restart: always

    depends_on:
      - postgres

    ports:
      - "8000:8000"

    environment:
      # Disable Azure CLI telemetry inside container
      AZURE_CORE_COLLECT_TELEMETRY: "false"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./:/workspace
      # Mount host Azure credentials so the container is already authenticated
      - ${USERPROFILE}/.azure:/root/.azure

volumes:
  postgres_data:
